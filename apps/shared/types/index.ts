import type { Task, TaskPriority, TaskStatus, Account, Comment, Session, Tag, User, Verification } from "../../server/prisma/generated/client"



export type {
    Task,
    TaskPriority,
    TaskStatus,
    Account,
    Comment,
    Session,
    Tag,
    User,
    Verification
}